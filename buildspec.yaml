version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies..."
      - npm install --legacy-peer-deps
  build:
    commands:
      - echo "Building the app..."
      - CI='' npm run build
artifacts:
  files:
    - '**/*'
  base-directory: dist
  discard-paths: no
